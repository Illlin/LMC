file_to_open = "test.py"
code = []
with open(file_to_open, "r") as program:
    for line in program:
        code.append(line)

key_word = {
    "print":f_print
}

def f_print(arg)
for line in code:
    
    